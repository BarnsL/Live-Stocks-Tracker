# macOS

Requires **Python 3** (pre-installed on most Macs, or get it from https://www.python.org).

```bash
git clone https://github.com/BarnsL/Live-Stocks-Tracker.git
cd Live-Stocks-Tracker
chmod +x platforms/macos/run.sh
./platforms/macos/run.sh
```
