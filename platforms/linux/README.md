# Linux

Requires **Python 3**:
```bash
sudo apt install python3 python3-venv python3-pip   # Debian/Ubuntu
```

```bash
git clone https://github.com/BarnsL/Live-Stocks-Tracker.git
cd Live-Stocks-Tracker
chmod +x platforms/linux/run.sh
./platforms/linux/run.sh
```
